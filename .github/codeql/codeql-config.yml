name: "CodeQL config"

queries:
  - uses: security-and-quality

# Java manual-build scope is controlled by the Maven command in codeql.yml.
# paths/paths-ignore do not restrict extraction in this build mode.
